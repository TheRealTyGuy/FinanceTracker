function AuthSubmitButton() {
    return (
        <button
            type="submit"
            className="w-full rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-500 focus:outline-2 focus:outline-offset-2 focus:outline-indigo-600"
        >
            Submit
        </button>
    );
}

export default AuthSubmitButton;