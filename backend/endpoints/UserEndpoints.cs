using Models;
using DTOs;

namespace Endpoints
{
    public static class UserEndpoints
    {
        public static void AddUserEndpoints(this WebApplication app)
        {
            // app.MapPost("/logout");
        }
    }
}